# ===========================================
# MULTIMODALITY LABELING SYSTEM - FRONTEND  
# ===========================================
# Copy this file to .env and fill in your actual values
# Note: All React environment variables must start with REACT_APP_

# ===========================================
# SUPABASE CONFIGURATION
# ===========================================
# Your Supabase project URL (same as backend)
REACT_APP_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (safe for client-side use, same as backend)
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# ===========================================
# API CONFIGURATION
# ===========================================
# Backend API URL - update based on your deployment
# Development: http://localhost:8000
# Production: https://your-api-domain.com
REACT_APP_API_URL=http://localhost:8000

# ===========================================
# OPTIONAL SETTINGS
# ===========================================
# Add any additional React app environment variables below
# Examples:
# REACT_APP_GA_TRACKING_ID=your-google-analytics-id
# REACT_APP_SENTRY_DSN=your-sentry-dsn
# REACT_APP_APP_VERSION=1.0.0

# ===========================================
# DEVELOPMENT NOTES
# ===========================================
# For development, you can also add:
# - FAST_REFRESH=false (if you encounter hot reload issues)
# - GENERATE_SOURCEMAP=false (to disable sourcemaps in build)
# - SKIP_PREFLIGHT_CHECK=true (to skip dependency version checks)

# ===========================================
# IMPORTANT SECURITY NOTES
# ===========================================
# ⚠️  NEVER commit your actual .env file to version control
# ⚠️  Only use REACT_APP_ variables for non-sensitive config
# ⚠️  Sensitive keys should only go in backend .env
# ⚠️  REACT_APP_ variables are embedded in the build and visible to users